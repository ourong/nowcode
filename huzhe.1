import sys

def getInput():
    return int(sys.stdin.readline().strip())

num = getInput()
while(num > 0):
    previous = -1
    count = 0
    counts = []
    winner = "NIUMEI"
    for i in range(num):
        current = getInput()
        if(current != previous):
            counts.append(count)
            previous = current
        else:
            count = count+1
    for i in reversed(counts):
        print(i)
        if(i % 2 == 1):
            winner = "NIUNIU"
            break
    print(winner)
    num = getInput()